---
plan_id: "08-02"
phase: 8
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/auth/Me.jsx
  - frontend/src/components/auth/EditProfileModal.jsx
  - frontend/src/components/Settings.jsx
  - frontend/src/components/Settings.css
  - frontend/src/components/ErrorStates.jsx
  - frontend/src/components/ErrorFallback.jsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
---

# Plan 08-02: Settings & Profile Polish + Error States

## Objective
Fix known bugs in Profile page, polish Settings page, rebuild error/empty states with proper dark mode and warm design tokens, and remove dead code. This makes Settings, Profile, and all error states production-ready with the warm earth-tone aesthetic.

## Tasks

### Task 1: Fix Profile page bugs

<read_first>
- frontend/src/components/auth/Me.jsx (full file)
</read_first>

<action>
In `frontend/src/components/auth/Me.jsx`:

1. **Remove debug boxShadow** (line 196):
   Change: `<motion.div className="h-full flex flex-col" style={{ boxShadow: "0px 0px 1px 0.1px #000000" }}>`
   To: `<motion.div className="h-full flex flex-col">`

2. **Fix brand name** (line 390):
   Change: `TaskFlow v1.0.0`
   To: `Taskly v1.0.0`
</action>

<acceptance_criteria>
- Me.jsx does not contain "boxShadow"
- Me.jsx does not contain "TaskFlow"
- Me.jsx contains "Taskly v1.0.0"
- `npm run lint` passes
</acceptance_criteria>

### Task 2: Polish Profile page design

<read_first>
- frontend/src/components/auth/Me.jsx (full file after Task 1 fixes)
- frontend/tailwind.config.js (warm color tokens)
</read_first>

<action>
In `frontend/src/components/auth/Me.jsx`, apply these visual refinements:

1. **Avatar container** (line ~225-226): Add a warm ring around the avatar:
   Change the avatar wrapper `<div className="relative w-28 h-28 flex-shrink-0">` to include `ring-4 ring-accent-primary/20 dark:ring-accent-primary/30 rounded-full`

2. **Stats cards** (lines ~265-301): Add subtle hover effect to stat cards:
   Add `hover:shadow-md transition-shadow duration-200` to each stat card div

3. **Profile header card** (line ~223): Add backdrop blur for depth:
   Add `backdrop-blur-sm` to the profile header card's className

4. **Section headings** (lines ~306, ~337, ~366): Add consistent spacing:
   Ensure all section headings use `text-lg font-serif font-semibold` (add `font-serif` if missing)

5. **Delete confirmation modal** (lines ~404-430): Style with warm tokens:
   Add `backdrop-blur-sm` to the overlay
   Add `shadow-xl dark:shadow-dark-lg` to the modal card
   Style Cancel button with warm border and hover: `border-light-border dark:border-dark-border hover:bg-light-bg-hover dark:hover:bg-dark-bg-hover text-light-text-primary dark:text-dark-text-primary`
</action>

<acceptance_criteria>
- Avatar has a warm-toned ring border
- Stat cards have hover shadow effect
- Profile header card has backdrop blur
- Section headings use serif font
- Delete modal uses warm dark mode tokens
- `npm run lint` passes
</acceptance_criteria>

### Task 3: Polish Settings page

<read_first>
- frontend/src/components/Settings.jsx (full file)
- frontend/tailwind.config.js
</read_first>

<action>
In `frontend/src/components/Settings.jsx`, apply these refinements:

1. **Section headers**: Add serif font to section titles:
   Change `<h2 className="text-xl font-semibold ...">` to `<h2 className="text-xl font-serif font-semibold ...">` (in SettingsSection component, line ~33)

2. **Toggle switch refinement**: Add focus ring for accessibility:
   In ToggleSwitch component, add `focus-within:ring-2 focus-within:ring-accent-primary/20 rounded-full` to the outer label

3. **Save toast styling**: Add backdrop blur and warm shadow:
   On the toast div (line ~406), add `backdrop-blur-md shadow-xl dark:shadow-dark-lg`
</action>

<acceptance_criteria>
- Settings section titles use `font-serif`
- Toggle switches have visible focus rings
- Save toast has backdrop blur and enhanced shadow
- `npm run lint` passes
</acceptance_criteria>

### Task 4: Delete Settings.css dead code

<read_first>
- frontend/src/components/Settings.css (verify it's not imported anywhere)
- frontend/src/components/Settings.jsx (verify no import of Settings.css)
</read_first>

<action>
Delete `frontend/src/components/Settings.css`. Verify first that no file imports it:
- Run: `grep -r "Settings.css" frontend/src/` — should return 0 results
- If the grep returns results, investigate before deleting
</action>

<acceptance_criteria>
- `frontend/src/components/Settings.css` no longer exists
- `grep -r "Settings.css" frontend/src/` returns 0 results
- Settings page renders identically without the CSS file
- `npm run lint` passes
</acceptance_criteria>

### Task 5: Rebuild ErrorStates.jsx

<read_first>
- frontend/src/components/ErrorStates.jsx (full file — 43 lines)
- frontend/tailwind.config.js (warm tokens, accent colors)
</read_first>

<action>
Rewrite `frontend/src/components/ErrorStates.jsx` with warm design tokens and dark mode support for all components:

1. **NotFound**: Add `text-light-text-primary dark:text-dark-text-primary` to heading and `text-light-text-secondary dark:text-dark-text-secondary` to paragraph. Add warm icon container with `p-4 rounded-2xl bg-accent-warning/10 dark:bg-accent-warning/20 mb-4` around icon. Remove bare `mb-4` from icon.

2. **ServerError**: Same pattern — add dark mode text colors. Replace `btn-primary` with Tailwind warm button: `px-5 py-2.5 bg-accent-primary hover:bg-accent-primary-dark dark:bg-accent-primary-light dark:hover:bg-accent-primary text-white rounded-xl font-semibold shadow-sm transition-all duration-200`. Add warm icon container.

3. **NetworkError**: Same pattern as ServerError. Add warm icon container.

4. **EmptyState**: Already has dark mode for border and text. Enhance icon container: wrap icon in `p-4 rounded-2xl bg-light-bg-tertiary dark:bg-dark-bg-tertiary mb-4`. Increase icon size to `w-10 h-10`. Add `font-serif` to message if it's a heading-style empty state.

5. **New: EmptyStateAction** — export a new variant with an action button:
```jsx
export const EmptyStateAction = ({ message, icon: Icon = FiInbox, actionLabel, onAction }) => (
  <div className="flex flex-col items-center justify-center h-full text-center p-6 border-2 border-dashed border-light-border dark:border-dark-border rounded-2xl">
    <div className="p-4 rounded-2xl bg-light-bg-tertiary dark:bg-dark-bg-tertiary mb-4">
      <Icon className="w-10 h-10 text-light-text-tertiary dark:text-dark-text-tertiary" />
    </div>
    <p className="text-light-text-secondary dark:text-dark-text-secondary mb-4">{message}</p>
    {actionLabel && onAction && (
      <button onClick={onAction} className="px-5 py-2.5 bg-accent-primary hover:bg-accent-primary-dark text-white rounded-xl font-semibold shadow-sm transition-all duration-200">
        {actionLabel}
      </button>
    )}
  </div>
)
```
</action>

<acceptance_criteria>
- All error components have `dark:text-dark-text-primary` on headings
- All error components have `dark:text-dark-text-secondary` on paragraphs
- No `btn-primary` class references remain in ErrorStates.jsx
- All icons wrapped in warm-toned container divs
- `EmptyStateAction` component exported with action button support
- `npm run lint` passes
</acceptance_criteria>

### Task 6: Fix ErrorFallback.jsx

<read_first>
- frontend/src/components/ErrorFallback.jsx (full file — 20 lines)
</read_first>

<action>
In `frontend/src/components/ErrorFallback.jsx`:

1. Replace `btn-primary` class on the retry button with warm Tailwind classes:
   `px-5 py-2.5 bg-accent-primary hover:bg-accent-primary-dark dark:bg-accent-primary-light dark:hover:bg-accent-primary text-white rounded-xl font-semibold shadow-md transition-all duration-200`

2. Add warm icon container around the heading area (optional — add a FiAlertTriangle icon above the heading):
   ```jsx
   <FiAlertTriangle className="w-12 h-12 text-accent-danger mb-4" />
   ```

3. Style the error pre block with warm tokens:
   Add `border border-accent-danger/20 dark:border-accent-danger/30` to the pre element
   Add `text-sm font-mono` for better readability
</action>

<acceptance_criteria>
- ErrorFallback.jsx does not contain "btn-primary"
- Retry button uses warm Tailwind classes with dark mode support
- Error pre block has warm border styling
- `npm run lint` passes
</acceptance_criteria>

## Verification
- Profile page shows "Taskly v1.0.0" (not "TaskFlow")
- Profile page has no debug boxShadow
- Settings page has serif section titles
- Settings.css is deleted
- All error/empty states work in both light and dark mode
- No `btn-primary` class references remain
- `npm run lint` passes
- `npm run build` succeeds
